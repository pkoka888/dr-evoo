// Script to (re)generate .devcontainer files.\n// For now it simply ensures the directory exists.\nconst fs = require('fs');\nconst path = require('path');\n\nconst target = path.join(__dirname, '..', '.devcontainer');\nif (!fs.existsSync(target)) {\n  fs.mkdirSync(target, { recursive: true });\n  console.log('Created .devcontainer directory');\n} else {\n  console.log('.devcontainer already exists');\n}\n
